import { describe, test, expect } from 'vitest'
import { getDeltaAngle } from './math'

// describe('getDeltaAngle function', () => {
//   test('with small angle', async () => {
//     const reason = 'error'
//     await expect(getDeltaAngle(reason)).resolves.toEqual()
//   })
// })
